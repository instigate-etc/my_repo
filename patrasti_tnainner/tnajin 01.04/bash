1.Write a Bash script program that extracts all numbers from the given file 
and returns sum of the extracted numbers.
2. Write a Bash script that takes two arguments signal type and PID and prints the corresponding type of signal name and terminates the running process.
3. Write a Bash script that takes file path and determines wheter it is directory or regular file, and if it is directory count all file count inside it. 
4. Write a  Bash shell script program that takes the name of a file or
directory as an argument and reports if the file is a directory, regular file
or other and also reports if the user has read, write and execute permission on
the file or directory.  Use a sequence of if statements on the file name to
determine the information.
5. Calculate GCD
